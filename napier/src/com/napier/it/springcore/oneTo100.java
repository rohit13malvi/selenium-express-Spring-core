package com.napier.it.springcore;

public class oneTo100 {
//	 public static void printNum(int num) {

	/*
	 * //If numbers is less than or equal to 100 if(num <= 100) {
	 * System.out.print(num + " ");
	 * 
	 * //Increment and call printNum recursively printNum(num+1); } }
	 */
	public static void main(String[] args) {
		int one = 'd' / 'd';
//		String str="My Name Is";
//		int hundred = 'd';

		String l = "          ";

		for (int i = one; i <= l.length() * l.length(); i++) {
			System.out.println(i);

//		}

			/*
			 * //Declare variable and assign a value 1 int n = 'a'/'a';
			 * 
			 * //call printNum method printNum(n);
			 */

		}

	}
}
